# zr_monitor

家庭网络监控脚本与数据集合（私密仓库）。

## 说明

本仓库包含：
- 监控采集脚本
- 设备信息导出文件
- 本地运行时状态、日志、历史 CSV 数据

由于包含大量本地运行数据，已设置 `.gitignore`，后续默认不再上传这些文件。

## 目录说明

- `*.py`：主要脚本
- `device_info.json`：本地设备信息快照
- `matrix_data/`：本地历史数据
- `__pycache__/`、`*.log`、`*.bak`、部分 `.json` 状态文件：运行时缓存与日志
